box: wercker-labs/docker
build:
    steps:
        - script:
            name: docker build
            code: docker build -t inventory .
        - script:
            name: docker run
            code: docker run -t inventory -d && sleep 5
        - script:
            name: test
            code: bundle install && bundle exec rspec --tag integration
