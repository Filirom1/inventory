box: wercker-labs/docker
build:
    steps:
        - script:
            name: docker build
            code: docker build -t inventory -f Dockerfile-unicorn .
        - script:
            name: docker run
            code: docker run -t inventory
        - script:
            name: test
            code: |
                  curl -XPOST localhost/api/v1/facts/MY_UUID -d '{"key": "value"}'
