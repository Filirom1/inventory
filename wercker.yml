box: wercker-labs/docker
build:
    steps:
        - script:
            name: docker build
            code: docker build -t inventory .
        - script:
            name: docker run
            code: docker run -d true -t inventory && sleep 5
        - script:
            name: integration tests
            code: docker run -w /app inventory rspec --tag integration
