#!/usr/bin/env ruby

require 'inventory/server'

server = Inventory::Server::Server.new(:port => 2525, :host => "127.0.0.1", :debug => ENV['DEBUG'], :maxConnections => 4)
server.start

BEGIN {
  at_exit {
    server.stop
  }
}
